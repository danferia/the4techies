---
- hosts: localhost
  become: true
  vars:
   server_name: 
  tasks:
   - name: Install Nginx
     apt: 
      name: nginx
      state: latest
      update_cache: yes
     notify:
      - start nginx
   - name: Latest Ngix Version
     apt: 
      name: nginx 
      state: latest
   - name: Start Nginx
     service:
      name: nginx
      state: started
